name: Publish Package

on:
  push:
    branches:
      - main

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: 14

    - name: Publish to GitHub Packages
      run: |
        npm config set @marco5dev:registry https://npm.pkg.github.com
        echo "//npm.pkg.github.com/:_authToken=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" > .npmrc
        npm publish

      env:
        GITHUB_USERNAME: marco5dev
